import { createTheme } from "@mui/material";

export const globalTheme = createTheme({
    typography: {
        fontFamily: 'Godo',
    },
    palette: {
        primary: {
            main: '#0e4a84',
        },
        secondary: {
            main: '#f08100',
        },
    }
})